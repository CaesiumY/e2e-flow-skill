// playwright.config.ts
// Generated by e2e-flow skill (Phase 1). Edit freely after generation.
// Placeholders ({{...}}) are substituted by the skill.
import { defineConfig, devices } from '@playwright/test';

const BASE_URL = process.env.E2E_BASE_URL ?? '{{BASE_URL}}';

export default defineConfig({
  testDir: './e2e',
  fullyParallel: true,
  forbidOnly: !!process.env.CI,
  retries: process.env.CI ? 2 : 0,
  workers: process.env.CI ? 1 : undefined,
  reporter: process.env.CI ? [['blob'], ['list']] : 'list',

  use: {
    baseURL: BASE_URL,
    // 첫 실패에도 trace.zip 보존 — Self-Healer가 trace를 분석해 분류·패치 제안
    trace: 'retain-on-failure',
    screenshot: 'only-on-failure',
    video: 'retain-on-failure',
    actionTimeout: 10_000,
    navigationTimeout: 30_000,
  },

  expect: {
    timeout: 5_000,
    // VRT 기본값 — 자세한 운영은 references/phase-4-enhance.md
    toHaveScreenshot: {
      threshold: 0.2,
      maxDiffPixelRatio: 0.005,
      animations: 'disabled',
    },
  },

  projects: [
    {
      name: 'chromium-desktop',
      use: { ...devices['Desktop Chrome'] },
    },
    // 모바일 분기가 필요하면 주석 해제
    // {
    //   name: 'chromium-mobile',
    //   use: { ...devices['Pixel 7'] },
    // },
  ],

  // 로컬 개발 서버 자동 기동 — 필요 없으면 webServer 블록 제거
  webServer: process.env.CI
    ? undefined
    : {
        command: '{{PACKAGE_MANAGER}} dev',
        url: BASE_URL,
        reuseExistingServer: true,
        timeout: 120_000,
      },
});
