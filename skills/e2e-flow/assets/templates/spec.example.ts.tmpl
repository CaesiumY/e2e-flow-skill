// e2e/tests/example/specs/landing.spec.ts
// Generated by e2e-flow skill (Phase 1).
// 첫 통과 확인용 최소 예시. 자유롭게 삭제·수정 가능.
import { test } from '../../../fixtures';
import { 랜딩_페이지_확인_플로우 } from '../flows/landing-flows';

test.describe('랜딩 페이지', () => {
  test('진입 시 메인 컨텐츠가 보인다', async ({ page, navigation }) => {
    await page.goto('/');
    await 랜딩_페이지_확인_플로우({ page, navigation });
  });
});
